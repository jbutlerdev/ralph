# Ralph Web UI - Environment Variables Template
#
# Copy this file to .env.local and configure as needed.
# .env.local is git-ignored and should never be committed.
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.

# WebSocket Configuration
# URL of the WebSocket server for real-time plan updates
# Format: ws://host:port or wss://host:port (for HTTPS)
# Example: ws://localhost:3002 or wss://your-domain.com
NEXT_PUBLIC_WS_URL=ws://localhost:3002

# Polling Fallback Configuration
# Interval in milliseconds to use for polling when WebSocket is unavailable
# Higher values reduce server load but increase update latency
NEXT_PUBLIC_POLL_INTERVAL_MS=5000

# Environment
# Sets the Node.js environment mode
# development: Enables debug features and detailed error messages
# production: Enables optimizations and minification
NODE_ENV=development

# Optional: Custom API Base URL
# If your API is at a different domain, specify the base URL here
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: Analytics
# Add your analytics tracking IDs here
# NEXT_PUBLIC_GA_ID=UA-XXXXXXXXX-X
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=your-domain.com
