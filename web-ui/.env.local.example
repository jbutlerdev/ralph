# Ralph Web UI Environment Variables

# Default polling interval for data refresh (in milliseconds)
# This controls how frequently the UI will poll the backend for updates
# Default: 5000 (5 seconds)
NEXT_PUBLIC_POLL_INTERVAL_MS=5000
